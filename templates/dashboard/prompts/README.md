# prompts/

> LLM-actionable prompts. One per `<stage>.md`.
> See `../../AUTHORING.md` §Folder structure.

v0.1.0 ships one prompt as a placeholder:

- `scaffolding.md` — generates a new section scaffold from a section spec
  (the kind of prompt an am-coder invokes mid-build).

Add more as the worked example forces them. Vendor-neutral recommended; cite
at least one non-default vendor per prompt when possible.
