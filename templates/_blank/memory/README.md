# memory/ — placeholder

> Prose contracts per role + topic. Filenames are monotonic and zero-padded.
> See `../../AUTHORING.md` Rules 2 + 3 + 6.

Empty by design. Add `01-<topic>.md`, `02-<topic>.md`, … as the worked
example forces new concerns. **Do not pre-write speculative files.**

Every file starts with the trigger line:

```
# NN · <topic> — USE THIS WHEN: <imperative one-liner>
```
