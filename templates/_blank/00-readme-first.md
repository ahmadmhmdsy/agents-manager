# 00-readme-first — placeholder

> Human orientation for the new template. Replace this paragraph with a
> one-paragraph description of what the template builds and the conventions it
> follows. See `../AUTHORING.md` for the rulebook.

## Sections (placeholder)

List the sections the new template builds, in order. Each gets a one-line
description. The exemplar uses 8 sections; yours can use fewer or more, but
each must be present in `skeleton/` and indexed in `INDEX.md`.
