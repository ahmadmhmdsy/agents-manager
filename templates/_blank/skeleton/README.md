# skeleton/ — placeholder

> Reference implementation. Edit, don't fork. See `../../AUTHORING.md` Rule 7.

Empty by design. Author after memory (so memory's hard rules are settled
first). The skeleton is the test oracle for memory's hard rules.

The exemplar at `../../cinematic-landing/skeleton/index.html` is the
canonical reference shape — 8 sections, each carrying a `data-section`
attribute, with the engine wired through vanilla HTML/CSS/JS.
