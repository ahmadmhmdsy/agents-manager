# prompts/ — placeholder

> LLM-actionable prompts. One per `<stage>.md`.
> See `../../AUTHORING.md` §Folder structure.

Empty by design. Add prompts as the worked example forces them. Vendor-neutral
recommended; cite at least one non-default vendor per prompt when possible.
