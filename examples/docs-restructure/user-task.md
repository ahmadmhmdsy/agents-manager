Move `api.md` into a new `docs/api/` subdirectory at `docs/api/api.md`, and split it into 3 files: `docs/api/users.md` (the 3 user endpoints), `docs/api/products.md` (the 1 product endpoint), and `docs/api/README.md` (an index that links to both).

Also update the root `README.md` to link to the new `docs/api/README.md` instead of `api.md`.

Delete the old `api.md`.

`CONTRIBUTING.md` stays where it is.
