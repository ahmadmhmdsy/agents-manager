# Plan (phased) — T-2026-06-28-001 (no-consecutive-h1 rule)

By am-planning. 1 phase, 2 tasks.

## Phase 1 — Implement rule + tests

### P1T1 — Add `no-consecutive-h1` rule to `src/rules.js`

**Files:**
- Modify: `src/rules.js` — append new rule object to the array.

**Done when:**
- A new rule object is appended with `name: 'no-consecutive-h1'`.
- The `check(text, findings)` walks the lines, detects two consecutive `/^# /` lines, and pushes `{rule, line, col, message}` for the second H1.
- Line number points at the second H1's actual line (not `i+1` blindly — use the real index).
- Existing tests still pass (`npm test` green).

### P1T2 — Add tests to `test/linter.test.js`

**Files:**
- Modify: `test/linter.test.js` — append 3 new test cases.

**Done when:**
- Test: clean markdown (one H1) → 0 findings.
- Test: two consecutive H1 lines → exactly 1 finding with `rule === 'no-consecutive-h1'`, line pointing at the second H1.
- Test: H1 then `## ` (different header level) → 0 findings (only `# ` matches).
- `npm test` shows 5 passing tests total (2 existing + 3 new).
