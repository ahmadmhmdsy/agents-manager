# Plan (high-level) — T-2026-06-28-001 (no-consecutive-h1 rule)

By am-planning. High-level shape of the work; phased details in `02_plan_phases_T-2026-06-28-001.md`.

## Goal
Add a new linter rule `no-consecutive-h1` that flags an H1 (`# `) followed immediately by another H1, with line/col pointing at the second H1, plus tests.

## Approach
- Single-phase, single-task implementation. Copy the existing `no-trailing-h1` rule's structure.
- All changes inside one file each (`src/rules.js`, `test/linter.test.js`).

## Self-score (1–5)

| Dimension | Score | Notes |
|---|---|---|
| Testability | 5 | Pure function, deterministic, easy unit tests |
| Scope | 5 | One rule, one test file, no scope creep |
| Dependencies | 5 | No new deps |
| Risks-covered | 4 | Off-by-one in line numbers considered; col=1 noted as N/A; H1-only (not `## `) clarified |

**Mean: 4.75 / 5** — clean small change.
