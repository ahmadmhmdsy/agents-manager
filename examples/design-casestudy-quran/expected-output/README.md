# Expected output — design-casestudy-quran

This directory exists to satisfy the agents-manager `examples-consistency` CI check (which requires every example to have at least one of `share/` or `expected-output/`).

**What this is NOT:** a worked deliverables package. The Quran app design system was built before `am-design` was formalized; there are no live design artifacts to ship.

**What this IS:** a stub pointing readers to where the actual retrospective lives:

- [`../README.md`](../README.md) — the case-study summary
- [`../lessons.md`](../lessons.md) — the 12 patterns that emerged
- [`../mapping.md`](../mapping.md) — how those patterns map to v0.9.0 am-design modes
- [`../retrospective.md`](../retrospective.md) — the 6-month postmortem
